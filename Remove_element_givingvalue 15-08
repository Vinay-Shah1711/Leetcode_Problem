class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i=0
        while(i<len(nums)):
            
            if(val==nums[i]):
                nums.pop(i)
            else:
                i=i+1
        print(len(nums))  /*It is used to find length of array for 3223 output 2 which is 22 */
